import { SkeletonElement, SkeletonWrapper } from "./styles";

const Skeleton = () => {
  return (
    <SkeletonWrapper>
      <SkeletonElement />
    </SkeletonWrapper>
  );
};

export default Skeleton;
